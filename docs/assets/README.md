# 截图维护

[返回项目首页](../../README.md) · [管理台与报表](../tui.md) · [开发指南](../development.md)

截图使用演示数据，不包含真实群资料或凭据。日期、提交号和群用量是示例值，无需随每次代码提交更新。

| 图片 | 展示内容 | 更新时机 |
| --- | --- | --- |
| [tui-overview.png](tui-overview.png) | 100×24 管理台总览 | 总览布局、指标、导航或键位发生变化 |
| [report-preview.png](report-preview.png) | HTML 报表上半部分 | 报表布局、指标名称或统计口径发生变化 |

## 核对管理台

在项目根目录执行：

```sh
bun run tui:preview overview 100 24 --plain
```

将输出与图片中的导航、指标、趋势、待办和底部键位逐项比较。去掉 `--plain` 可查看颜色；演示数据和渲染入口位于 [preview.ts](../../scripts/ops/tui/preview.ts)。该命令只输出文本，不生成 PNG，也不读取真实 `data/`。

需要更新图片时，从同一渲染入口捕获 100×24 的画面，保留“演示数据”说明。不要手工改写截图中的界面文字或数值。

## 核对报表

报表生成逻辑位于 [report.ts](../../scripts/ops/tui/report.ts)。需要更新图片时，使用演示数据调用 `writeReport` 导出 HTML，在浏览器中打开后截图，保留与现有图片一致的内容范围。

当前图片只展示提问总量、各群提问次数、群概况和模型用量；完整报表还包含其他明细。替换图片后，在首页和[管理台指南](../tui.md#导出-html-报表)中检查清晰度与图注是否匹配。
